schema = "daodan/v1"
name = "ai-tooling"
version = "5.4.1"
description = "AI development workflow: prompt engineering knowledge base and optimization, and Claude Agent SDK integration guide"
license = "MIT"

[capabilities]
required = ["repository.read", "shell.execute", "repository.write", "network.fetch", "contexts.isolate", "roles.dispatch"]
optional = []

[dependencies]
required = ["superpowers@claude-plugins-official"]

[components]
skills = ["agent-sdk-builder", "prompt-engineering"]
roles = ["prompt-engineer"]
workflows = ["prompt-optimize"]
policies = []
