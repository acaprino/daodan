name = "firefox-scaffold"
entrypoint = "firefox-scaffold.md"

[[phases]]
id = "scope"
isolation = "shared"
produces = ["artifact:scope"]

[[phases]]
id = "review"
needs = ["scope"]
role = "firefox-extension-dev"
isolation = "required"
consumes = ["artifact:scope"]
produces = ["artifact:firefox-scaffold-report"]

[contract]
inputs = ["repository"]
outcomes = ["firefox-scaffold-completed"]
artifacts = ["firefox-scaffold-report"]
schemas = []
