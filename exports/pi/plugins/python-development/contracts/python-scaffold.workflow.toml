name = "python-scaffold"
entrypoint = "python-scaffold.md"

[[phases]]
id = "run"
isolation = "shared"
produces = ["artifact:python-scaffold-report"]

[contract]
inputs = ["repository"]
outcomes = ["python-scaffold-completed"]
artifacts = ["python-scaffold-report"]
schemas = []
